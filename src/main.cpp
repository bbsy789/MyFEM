#include <pre.h>
#include <common.h>
#include <matrix.h>
#include <solve.h>
#include <post.h>
#include <point.h>
#include <base.h>
#include <element.h>
#include <vector>

int main(int argc, char *argv[])
{
	vector<MATRIX*> V(NW,NULL);
	//定义存放MATRIX*类型的数组 V。
	//调用构造函数，此时vector容器内有NW个MATRIX*类型的变量，每个变量的值为NULL

}